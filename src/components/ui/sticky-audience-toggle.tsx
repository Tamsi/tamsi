'use client'

import { AudienceToggle } from '@/components/ui/audience-toggle'

/** Bottom-centered Human / Machine switch, always on screen (sinas.co pattern). */
export function StickyAudienceToggle() {
  return <AudienceToggle sticky />
}
